# Codebase Map — Add UI/UX design capability to e2e-engineering flow

## §Index
§1: L10–L36
§2: L38–L47
§3: L49–L66
§4: L68–L80
§5: L82–L88

## §1 Blast-radius modules
NEW — canonical source (`skills/e2e-engineering/`):
- `standards/ui-design.md` — generic anti-slop standard (mirror `standards/api-testing.md`).
- `schemas/design.md` — DESIGN.md schema, Stitch format (mirror `schemas/architecture.md`).
- `pre-impl/design.md` — conditional pre-impl sub-skill (mirror `research.md`/`prototype.md`).
- `agents/product-designer.md` — design advisor spec (mirror `agents/backend-architect.md`).
NEW — repo root:
- `docs/adr/0030-ui-design-system-and-design-advisor.md` — decision record.
- NOTE: `DESIGN.md` (repo root) is NOT created here. This repo has no UI surface. DESIGN.md is the per-project instance the new `design` sub-skill seeds LATER for UI tasks (analogy: ARCHITECTURE.md). Deliverable = schema + sub-skill that produce it, not the instance.
MODIFIED — canonical:
- `agents.manifest.json` (+`product-designer` role; refine `frontend-reviewer` desc → "slice reviewer").
- `agents/frontend-reviewer.md`; `pre-impl/grill-with-docs.md`; `pre-impl/to-prd.md`; `pre-impl/prototype.md`; `impl/tdd.md`.
MODIFIED — entry points (lockstep, both runtimes):
- `.claude/skills/e2e-engineering/SKILL.md`; `.agents/skills/e2e-engineering/SKILL.md`.
- `.claude/skills/e2e-flight/SKILL.md`; `.agents/skills/e2e-flight/SKILL.md`.
MODIFIED — durable + routers:
- `CONTEXT.md`; `README.md`; `AGENTS.md`; `.cursor/rules/e2e-engineering.mdc` (only if it enumerates pre-impl sequence).
GENERATED — do NOT hand-edit:
- `.claude/agents/product-designer.md` (via `generate-agent-wrappers.ps1`); `dist/**` (via `build-dist.js`).

## §2 Seams (where verification attaches)
- `scripts/validate.js` — PRIMARY gate. Checks: dist freshness · markdown-link integrity · JSON validity · deprecated-role guard (`ui-designer`/`senior-qa`, validate.js:122 + forbiddenPaths:142–153) · codex dist embedding (`validateCodexDist`).
- `scripts/build-dist.js` — syncs `dist/{claude,codex,marketplace,cursor}`; codex embeds sub-skills + path-rewrites links.
- `scripts/generate-agent-wrappers.ps1` — reads `agents/*.md` + manifest → emits `.claude/agents/<role>.md`.
- NO Vitest/Playwright. NO unit/API/UI tests apply. Gate = `npm run build && npm run validate` → `validate: ok`.

## §3 Local impact (consumers)
- SKILL.md ×2 (eng): pre-impl sequence line +`[design?]`; to-prd expert bullet → `product-designer`; durable-docs para +DESIGN.md.
- e2e-flight SKILL.md ×2: Step 3 fan-out inject +`ui-design.md`+scoped DESIGN.md for `ui` slices; ui review wave vs DESIGN.md.
- `to-prd.md`: swap UI advisor `frontend-reviewer`→`product-designer`; read DESIGN.md; capture audience+anti-refs → PRD; empty-DESIGN.md red flag (mirror §4.1 stall).
- `grill-with-docs.md`: +`design?` trigger (explicit YES/NO).
- `tdd.md`: `ui` slice +`ui-design.md`+scoped DESIGN.md inject; review line notes `frontend-reviewer` vs DESIGN.md.
- `prototype.md`: ui-branch refs `ui-design.md` + feeds DESIGN.md; image-gen allowed throwaway here only.
- `frontend-reviewer.md`: checks built slice vs `ui-design.md` (7 areas) + DESIGN.md (deviation=Important); division note.
- `README.md`/`AGENTS.md`/`.mdc`: pre-impl sequence + skills list + expert-agents line.
- `dist/**`: fully regenerated by build (not hand-edited).

## §4 Existing language (reconcile → CONTEXT.md)
REUSED terms (already in repo): expert agent · advisor · reviewer (`frontend-reviewer`) · standard (`api-testing.md`) · schema · durable doc (ARCHITECTURE.md/CONTEXT.md/constitution) · Fork Y (UI manual, no in-flight browser) · enforcement layer · anti-pattern · §Index offset/limit · dual-runtime · lockstep entry points.
NEW terms (seed CONTEXT.md glossary): register (Brand vs Product) · Creative North Star · design system (`DESIGN.md`) · design token · anti-slop · design advisor (`product-designer`).
LANDMINE: literal `ui-designer` is BANNED (validate.js guard). Use `product-designer` / "the design advisor". Substring `ui-design` is fine.

## §5 Refactor candidates  [NOT THIS TASK]
- README/SKILL advertised a "UI designer" advisor that never existed — THIS task closes that gap (in-scope, not a walled candidate).
- `frontend-reviewer` manifest desc "UI/UX designer reviewer" — pre-existing imprecision, refined in-scope (S3), not walled.
- None other surfaced. (`dist/**` duplication is generated output, not a candidate.)
